{
  "name": "anatolia-icons",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Türk kurum ve markalarının optimize SVG logoları ve React ikon bileşenleri.",
  "license": "MIT",
  "scripts": {
    "optimize": "node scripts/optimize.mjs && node scripts/build-readme-previews.mjs && node scripts/build-readme-catalog.mjs",
    "build:react": "node scripts/build-react.mjs",
    "build": "pnpm optimize && pnpm build:react && pnpm --filter @anatolia-icons/react build",
    "playground": "pnpm --filter anatolia-icons-playground dev",
    "pack:check": "pnpm --filter \"./packages/*\" pack --dry-run",
    "publish:packages": "pnpm --filter \"./packages/*\" publish --access public"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@10.15.1",
  "devDependencies": {
    "@svgr/core": "^8.1.0",
    "@svgr/plugin-jsx": "^8.1.0",
    "fs-extra": "^11.3.1",
    "svgo": "^3.3.2",
    "typescript": "^5.9.2"
  }
}
